# Configuración mínima de Prometheus para KOS.
# Los targets de api/workers se añaden en el Sprint 1, cuando expongan /metrics.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
