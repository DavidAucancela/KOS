# Alembic — migraciones de Postgres. packages/core es el único dueño del esquema (doc 10 §9).
# Uso: uv run alembic -c packages/core/alembic.ini upgrade head

[alembic]
script_location = %(here)s/src/kos_core/alembic
prepend_sys_path = .
# La URL sale de kos_core.config (variables de entorno), ver env.py.
sqlalchemy.url =
